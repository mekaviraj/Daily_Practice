class Solution:
    def check(self, nums: List[int]) -> bool:
            rot = 0
            n = len(nums)
            for i in range(n):
                if nums[i] > nums[(i + 1) % n]:
                     rot += 1
            return rot <= 1
